package lesson22.functionalInterface;

public interface Sravnivaemyj {
	
	/**
	 * @param o other object
	 * @return negative if this < o, positive if this > 0, 0 if equals
	 */
	int compare(Object o);
	
}
